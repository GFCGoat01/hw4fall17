%h1 Create New Movie

= form_tag movies_path do

  = label :users, :title, 'User-ID'
  = text_field :users, 'User-ID'

  = label :users, :email, 'E-Mail'
  = text_field :email, 'E-Mail'

  = submit_tag 'Create my account'
